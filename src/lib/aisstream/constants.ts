/** AISStream subscription bounding boxes — [lat, lon] corner pairs. */
export const AIS_SUBSCRIPTION_BOUNDING_BOXES: [number, number][][] = [
  [[0, 45], [35, 105]],
];

export const MAX_RETURNED_SHIPS = 100;

export const INDIA_COAST_DISTANCE_KM = 500;
