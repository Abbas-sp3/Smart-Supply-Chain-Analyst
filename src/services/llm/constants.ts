/**
 * LLM layer constants
 */

/** Default request timeout per provider attempt (ms) */
export const LLM_REQUEST_TIMEOUT_MS = 55_000;

/** Stable Gemini model for reasoning + structured JSON output */
export const GEMINI_MODEL = "gemini-2.5-flash";

export const GEMINI_API_BASE =
  "https://generativelanguage.googleapis.com/v1beta";
